import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "AI Tower of Hanoi Solver — Color-Constrained State-Space Search",
  description:
    "An AI-powered solver for the modified Tower of Hanoi problem with color constraints. Uses LangGraph and BFS to find optimal solutions while visualizing the search process in real time.",
  keywords: ["Tower of Hanoi", "AI", "BFS", "State-Space Search", "LangGraph", "Color Constraints"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
